* ALL
  - mmdebstrap >= 0.8.0 (not in bullseye/stable): use --format=null

* collections/base
  - fixup man page permissions
    ?? chown -R man:man /var/cache/man

* collections/kvm
  - initramfs first-boot hook
    - cloud-init?
    - first-boot rewrite UUID, PARTUUID, disk-uuid?
    - first-boot resize rootfs
    - first-boot hostname?
    - first-boot machine-id
    - first-boot host SSH keys
  - use shim + grub-efi-amd64-signed?
  - smaller kernel image?
    (/lib/modules is huge, considering that most modules are not used at all)
