config file paths:

overlay/customize/etc/nftables.conf.dist

  Specific firewall configuration for the rootfs.

overlay/customize/etc/nftables.conf.initramfs

  Early firewall configuration.

  Also used as default for /etc/nftables.conf
  if no specific config file exists.
